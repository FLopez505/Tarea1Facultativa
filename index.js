/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Init from './src/uicomponets/init'
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Init);
